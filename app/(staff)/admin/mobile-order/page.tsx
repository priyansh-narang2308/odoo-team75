import { MobileOrderSettings } from "@/components/admin/mobile-order-settings";

export const metadata = {
  title: "Mobile Order Settings",
};

export default function MobileOrderPage() {
  return <MobileOrderSettings />;
}
